name: Build Test

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install --legacy-peer-deps

      - name: Build
        run: npm run build
        env:
          NEXT_PUBLIC_SUPABASE_URL: "https://cuzxsiefsgkywfofqrwx.supabase.co"
          NEXT_PUBLIC_SUPABASE_ANON_KEY: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImN1enhzaWVmc2dreXdmb2Zxcnd4Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODI2NDY2MDksImV4cCI6MjA5ODIyMjYwOX0.d66R7vXOmnCmtxh8EqFH9v1nxIXTrItIVypsSPoodVA"
          NEXT_TELEMETRY_DISABLED: "1"
          NODE_OPTIONS: "--max_old_space_size=4096"
